# Personal OS ユーザーガイド

## 再構築版の使い方

入力はまず原文・画像として保存され、解析はバックグラウンドの `analysis_jobs` で実行されます。画面の解析状態で未解析/実行中/完了/失敗を確認し、設定したローカルLLMが停止している場合も、許可していない限りクラウドへ送信されません。

Factの詳細な根拠は、確認画面から会話・画像・抽出モデルを開いて確認できます。現在有効な値はCurrent、置き換えられた値は履歴として残ります。Todayの「次の選択肢」から提案を作成し、「この案で判断」でDecisionとPlanへ進めてください。提案は実行を行いません。

バックアップ設定では世代を削除せず保持します。必要な場合は `GET /api/backup/verify?path=data/backups/<file>.posbackup` でDB・添付ファイル・manifestの整合性を検証できます。復元APIは事前バックアップ作成と `confirm=true` を必須とします。

Personal OSは、会話・メモ・画像・判断履歴を保存し、自分の現在情報を参照しながら相談できるローカル優先のアプリです。

起動後は「今日」が入口です。最上部の「相談する」「記録する」に続き、今日の一言、次にやること、最近変わったこと、思い出しておくこと、相談候補を確認できます。記憶状況、取込、設定、チェックイン、質問セットなどは「その他の機能」から開きます。

## 1. 起動する

PowerShellでプロジェクトフォルダを開き、次を実行します。

```powershell
cd personal-os
.\tools\start_production.ps1
```

本番は `data/personal_os.db` と固定ポート `8787` を使用します。ブラウザで `http://localhost:8787` を開きます。

同じWi-FiのiPhoneからは、起動時に表示されるLAN URLをSafariで開きます。Safariの共有メニューから「ホーム画面に追加」するとPWAとして使えます。

## 2. 最初に設定すること

### ローカルLLM

Ollamaをインストールし、モデルを取得します。

```powershell
winget install --id Ollama.Ollama --exact
ollama pull qwen3.5:9b
```

設定画面で次を指定します。

- ローカルLLMモデル: `qwen3.5:9b`
- ローカルLLM URL: `http://127.0.0.1:11434/v1`
- 相談用LLM、抽出用LLM: `ローカルLLM`

Ollamaが起動していれば、通常の相談、メモ抽出、ChatGPT取込、画像抽出をローカルで実行できます。APIキーは画面やDBには保存されません。

### クラウド送信の安全設定

設定画面のクラウドfallbackは初期状態ではオフです。特にChatGPT一括取込とセンシティブ情報のクラウド送信は、明示的に許可するまで外部へ送信されません。

## 3. ChatGPTエクスポートを取り込む

1. ChatGPTからデータエクスポートZIPを取得します。
2. Personal OSの「取込」タブを開きます。
3. ZIPを選択して取り込みます。
4. 「その他の機能」から「記憶状況」を開き、解析Jobの進捗を確認します。

会話本文は原文として保存され、AIが抽出した構造化情報は正本テーブルの `facts` に保存されます。抽出された資産・収入・人物など重要な情報は、確認提案として表示される場合があります。

ZIPはサーバー側の一時ファイルへ逐次保存され、会話JSONの配列も一件ずつ読み取ります。250会話ごとに確定し、分割ファイル単位でcheckpointします。同じZIPを再度送っても`external_id`で重複を抑え、途中失敗した取込は完了済み分を再利用して続行します。取込履歴は `GET /api/import-jobs` で確認できます。

## 4. 解析を管理する

「記憶状況」には次が表示されます。

- 未解析件数
- 解析中件数
- 完了件数
- 失敗件数
- 進捗率
- 使用中のProviderとモデル

「未解析」は、現在の抽出Provider・モデル・Prompt versionで処理する、有効な会話単位または添付画像のうち、まだ処理していない件数です。過去モデルのJob、無効になった旧チャンク、一般知識など事前判定でPersonal OSの対象外になった単位は含みません。「件数の根拠と解析設定」を開くと、取込会話数、有効な会話単位、現在の解析対象、LLM対象外、過去設定のJobを分けて確認できます。

「解析を一時停止」は、実行中の1件が完了してから新しい解析を止めます。「解析を再開」で再開できます。「GPUメモリを解放」は解析を停止し、Ollamaのモデルをメモリから解放します。

設定画面の「1サイクルの解析件数」は、連続処理中に何件ごとにJob一覧を更新するかを指定します。通常は100件を推奨します。PC操作中に負荷を抑えたい場合は25または50件、解析を優先する場合は200件を選べます。LLMへの同時要求数は増えないため、変更によってVRAMが二重に必要になることはありません。

Provider・モデル・prompt versionを変更した後は、記憶状況の再解析操作で古い解析結果だけを再キューできます。現在の設定と一致する完了済みJobは再処理しません。

## 5. 普通のメモを保存する

「記憶」タブの入力欄に自然文で入力します。

例:

```text
今月はExampleCorpの持株会に12,345円入れた
```

AIはカテゴリ、種類、金額、資産名、日付、Evidence引用などを抽出します。原文Evidenceが十分で内容が明確なFactはカテゴリに関係なく自動確定され、Evidence不足・矛盾だけが確認候補になります。

## 6. スクリーンショットを取り込む

### ファイル選択

「記憶」タブの「スクリーンショットから記録」で、PNG・JPEG・WebP画像を選択します。12MBまで対応しています。

### PCのクリップボード貼り付け

1. `Shift + Win + S` でスクリーンショットを撮ります。
2. Personal OSの「記憶」タブを開きます。
3. `Ctrl + V` を押します。
4. 必要なら「資金の情報」「旅行の予定」「住居候補」など補足を入力します。空欄でも画像内容から候補カテゴリーを判断します。
5. 「画像から情報を抽出」を押します。

画像は押した時点で `data/attachments/` にローカル保存され、解析Jobは非同期に進みます。最初にローカルOCRを実行し、文字量と信頼度が十分ならOCR文字列を通常のFact抽出へ渡します。表や画面構造など画像理解が必要な場合だけOllama Visionへフォールバックします。画像本体を外部LLMへ自動送信することはありません。

保存した画像と抽出結果は、記憶タブの「画像の記憶」で確認できます。OCRの結果・engine・信頼度も表示されます。削除時は先に対象Fact・Evidence・派生データ・画像本体の件数をプレビューし、確認後にまとめて削除します。

健康情報は、本人が明示した症状・薬・受診・検査値・医師から明示された診断をEvidence付きで自動確定できます。AIが病名を推測した場合はFact化しません。人物情報も、本人・相手が明示した事実は自動確定し、性格・好感度・心理状態などのAI推測はFact化しません。カテゴリだけを理由に確認を求めることはなく、重要でもEvidence不足・矛盾が解消できない場合だけ確認対象になります。

## 検証用DBでMigration・解析を確認する

本番の大量履歴を毎回解析しないため、検証用の小さなDBを作成できます。元の `data/personal_os.db` は変更されません。

```powershell
python tools/create_verification_db.py --source data/personal_os.db --output data/verification/personal_os_verification.db --entries 8 --chunks-per-entry 2 --analysis-jobs 20
cd personal-os
.\tools\start_verification.ps1
```

検証は `data/verification/personal_os_verification.db` と固定ポート `8877` を使用します。検証DBでは原文8件・各2チャンク・解析Job最大20件を目安にします。`PRAGMA integrity_check`、Migration、解析、画面確認、テストが通ったコードだけを本番へ反映してください。検証DBのデータを本番へ自動コピーすることはありません。

## 7. 記憶を確認・修正する

- 「今日」: 現在の真実、最近の更新、確認待ち提案
- 「確認」: factの信頼度、カテゴリ、根拠会話、抽出モデルを確認
- 「記憶状況」: 記録種類、抽出カテゴリ、資産取引、解析進捗
- 「質問セット」: 旅行・資産・生活などの選択式情報を保存

重要なfactは「確認」「却下」「後で確認」から選べます。現在の情報を更新すると、古いfactは履歴として残り、新しいfactが現在情報になります。

抽出内容そのものが誤っている場合は「内容を修正」を使います。修正前後の値は `memory_corrections` に監査履歴として残り、現在情報・信頼度・金融集計が再評価されます。

### 今日のパーソナルダイジェスト

「今日」のダイジェストは、確認済みFact、明示した判断、結果、更新履歴だけから作ります。心理状態やAIの推測を事実のように表示・保存することはありません。

- 「次にやること」は最大3件で、結果待ち → 実行待ち → 判断待ち → 後日評価待ちの順です。
- 「最近変わったこと」は最大3件、「思い出しておくこと」はEvidenceのある記憶を最大2件だけ表示します。機微な分野の本文は一覧で伏せます。
- 「根拠を見る」から関連する記録・判断へ移動できます。
- 「この内容で相談する」は相談画面に文面を入れるだけです。自動送信も、Fact・Recommendation・Planの自動作成もしません。
- 記録が少ない場合は空のダイジェストを表示し、「記録する」「相談する」から始められます。

## 8. 専用画面を使う

- 「資産」: 現在の資産fact、取引、投資関連の判断。確認済み残高から総資産、資産種別、月間積立を集計
- 「旅行」: 訪問地、希望地、旅行要素、旅行判断。訪問地、ホテル、費用、マイルの登録状況を集計
- 「住居」: 家賃、広さ、希望条件、引っ越し判断。候補物件を現在の住居と並べて確認
- 「人間関係」: 確認済み人物factと会話タイムライン
- 人物画面には `entity_type=person` と判定された明示的な関係だけを表示します。キャラクター、作品、プロジェクト、企業、場所などのMentionはPeopleから除外され、元の原文と訂正履歴は保持されます。
- 「判断」: 判断、選択肢、理由、結果、後日評価

これらは別の複製DBではなく、`facts`・`entities`・`decisions`・原文から生成される表示です。人物は人物カードの削除操作から、Fact・Evidence・関連indexを含む対象件数を先に確認できます。プライバシー削除は対象を限定して実行し、`GET /api/privacy/delete/preview` で削除範囲、`GET /api/privacy/audit` で実行履歴を確認できます。

### 金融取引の見方

Money画面の「適格な取引」は、本人が実際に行った取引だけです。シミュレーション、企業業績、株価、物件価格、1株配当、将来予測などは金融factとして残っても取引総額には含まれません。取引候補が曖昧な場合は「確認待ち」に表示され、「取引として承認」「取引ではない」を選べます。金額は円に正規化され、監査情報から元の金額表現・根拠会話・除外理由を確認できます。

## 9. 相談時の不足情報

相談すると、現在の確認済みfact・関連する判断・過去記憶を根拠として表示します。質問内容に対して重要だが未登録の前提（例: 引っ越し相談なら家賃や月間積立、旅行相談なら予算や日数）がある場合は、「不足している前提」として最大3件表示します。追加入力は任意で、候補を表示しただけでは保存されません。

## 10. バックアップと終了

DBは `data/personal_os.db`、添付画像は `data/attachments/`、完全バックアップは `data/backups/*.posbackup` に保存されます。各世代はSQLite DB、添付画像、manifest、schema migration一覧、各ファイルのSHA-256を含みます。起動直後は無条件に作成せず、前回から24時間経過した時点で一時bundleへ書いて検証後に確定します。前世代は削除されません。

設定画面の「今すぐバックアップ」で手動作成できます。復元時は現在状態の事前バックアップを作り、bundle全体の検証成功後にだけ切り替えます。同じ設定画面の「Personal OSを終了」でWebサーバーと解析Workerを安全に停止できます。

## 10. 困ったとき

### Local LLMがNot foundになる

設定値を確認します。

```powershell
ollama list
ollama pull qwen3.5:9b
```

URLは `http://127.0.0.1:11434/v1`、モデル名は `qwen3.5:9b` と完全一致させます。Ollamaを起動した後、Personal OSを再読み込みしてください。

### 解析が進まない

「記憶状況」で一時停止状態を確認し、「件数の根拠と解析設定」内の「解析を再開」を押します。Providerが設定済みであること、Ollamaが起動していること、失敗件数が増えていないことを確認します。

### ポートが使用中

本番 `8787` または検証 `8877` の既存画面を開き、設定画面の「Personal OSを終了」で終了してから、対応する起動スクリプトを再実行します。DBの取り違えを防ぐため、別ポートでは起動しません。

```powershell
.\tools\start_production.ps1
# または
.\tools\start_verification.ps1
```

### データをGitHubに上げるとき

非公開リポジトリでも、`data/`、`.env`、DB、バックアップ、画像、APIキーはコミットしないでください。プロジェクトの `.gitignore` で除外済みです。

commit前には次のローカル検査も実行できます。秘密値そのものは出力しません。

```powershell
python tools/check_secrets.py
```

### 動作状態をまとめて確認する

`GET /api/health` はDBのquick check、解析queueと最終処理時間、古いrunning Job、バックアップ世代、bind/CORS境界を返します。異常時は、画面表示だけでなくこの応答も確認してください。
## 会話の再分割と記憶品質

ChatGPT取込データは、ユーザー発言と直後のアシスタント応答を1単位として解析します。次のユーザー発言は別チャンクになり、一般知識だけのチャンクはローカルのPersonal relevance判定でLLM解析対象から除外されます。

検証画面の「会話を発言単位で再解析」から、既存の粗い根拠を隔離して再解析できます。原文・旧チャンク・Factの訂正履歴は削除されません。

## 日常画面（Phase 5）

「今日」では、最初に「相談する」と「記録する」を使えます。その後に今日の一言、次の対応、最近の変化、思い出しておくこと、相談候補を表示します。スマホでは下部ナビゲーションの「＋」から、記録・支出/資産・旅行・人物・判断を選べます。

記憶の入力中は本文が下書きとして残ります。「保存しています…」の後、サーバーが保存を確認できた場合だけ「保存しました」と表示され、下書きは削除されます。通信エラー時は「保存できませんでした。入力内容は保持しています」と表示されるため、内容を直して再送してください。画像追加も同じ入力画面から行えます。

資産、旅行、住居、人間関係は共通して「現在の要約」「最近の変化」「関連する判断」「履歴」「根拠」の順で確認します。人物の本文や機微な根拠は一覧に過剰表示せず、必要なときだけ根拠を開いて確認してください。

「判断」では、未完了の判断から結果を記録できます。Sheetを閉じても結果入力の下書きは残り、再度開いて保存できます。「探索」にはPersonal Spaceと人口ベンチマークがあります。ベンチマークの比較データ追加はSheet内で行い、個人Factを外部へ送信しません。

開発・検証向けの画面例は [UX Phase 5 Visual Review](docs/ux_phase5_visual_review.md) を参照してください。掲載画像はすべて合成データです。
