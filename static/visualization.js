/* Local-only Explore surfaces. No third-party visualization dependency. */
(function () {
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
  const domains = { finance: '資産', money: '資産', travel: '旅行', housing: '住居', relationship: '人間関係', people: '人間関係', work: '仕事', health: '健康', life: '生活', learning: '学習', hobby: '趣味', food: '食事', shopping: '買い物', technology: '技術', other: 'その他' };
  const domainLabel = domain => domains[domain] || 'その他';
  const nodeKindLabels = { fact: '記憶', decision: '判断', recommendation: '提案', plan: '計画', result: '結果' };
  const temporalLabels = { current: '現在', historical: '履歴', history: '履歴' };
  const nodeKindLabel = kind => nodeKindLabels[String(kind || '').toLowerCase()] || '記録';
  const temporalLabel = value => temporalLabels[String(value || '').toLowerCase()] || 'その他';
  let space = { nodes: [], edges: [], colors: {} }, selectedId = null;
  let camera = { yaw: .16, pitch: -.18, zoom: 1, panX: 0, panY: 0 };
  const hash = text => { let n = 2166136261; for (const ch of String(text)) n = Math.imul(n ^ ch.charCodeAt(0), 16777619); return n >>> 0; };
  function position(node) { const n = hash(node.id), angle = (hash(node.domain) % 12) * Math.PI / 6, radius = .34 + (n % 29) / 100; return { x: Math.cos(angle) * radius + ((n >>> 8) % 21 - 10) / 180, y: Math.sin(angle) * radius + ((n >>> 13) % 21 - 10) / 180, z: ((n >>> 18) % 100 - 50) / 100 }; }
  function project(node, width, height) { const p = position(node), cy = Math.cos(camera.yaw), sy = Math.sin(camera.yaw), cp = Math.cos(camera.pitch), sp = Math.sin(camera.pitch), x = p.x * cy - p.z * sy, z = p.x * sy + p.z * cy, y = p.y * cp - z * sp, depth = 1.4 + z * .45; return { x: width / 2 + camera.panX + x / depth * width * .76 * camera.zoom, y: height / 2 + camera.panY + y / depth * height * .76 * camera.zoom, depth }; }
  function activeNodes() { const query = ($('space-search')?.value || '').trim().toLowerCase(), domain = $('space-domain')?.value || '', kind = $('space-kind')?.value || '', current = $('space-current')?.checked, history = $('space-history')?.checked, mobile = matchMedia('(max-width:767px)').matches; return space.nodes.filter(node => { const isCurrent = node.temporal_bucket === 'current'; return (!isCurrent || current) && (isCurrent || history) && (!domain || node.domain === domain) && (!kind || node.kind === kind) && (!query || `${node.label} ${node.domain} ${node.kind}`.toLowerCase().includes(query)); }).slice(0, mobile ? 70 : 220); }
  function drawShape(ctx, node, radius) { if (node.kind === 'decision') { ctx.rotate(Math.PI / 4); ctx.rect(-radius, -radius, radius * 2, radius * 2); return; } if (node.kind === 'plan') { ctx.rect(-radius, -radius, radius * 2, radius * 2); return; } ctx.arc(0, 0, radius, 0, Math.PI * 2); }
  function renderSpace() { const canvas = $('personal-space-canvas'); if (!canvas) return; const ratio = Math.min(devicePixelRatio || 1, 2), rect = canvas.getBoundingClientRect(), width = rect.width, height = rect.height; canvas.width = Math.max(1, Math.floor(width * ratio)); canvas.height = Math.max(1, Math.floor(height * ratio)); const ctx = canvas.getContext('2d'); ctx.scale(ratio, ratio); ctx.fillStyle = '#070d12'; ctx.fillRect(0, 0, width, height); const nodes = activeNodes().map(node => ({ ...node, point: project(node, width, height) })).sort((a, b) => a.point.depth - b.point.depth), shown = new Map(nodes.map(node => [node.id, node])); if ($('space-edges')?.checked) space.edges.forEach(edge => { const from = shown.get(edge.from), to = shown.get(edge.to); if (!from || !to) return; ctx.save(); ctx.globalAlpha = edge.kind === 'lifecycle' ? .55 : .3; ctx.strokeStyle = edge.kind === 'result' ? '#8ce8bb' : '#93a3a0'; ctx.lineWidth = edge.kind === 'lifecycle' ? 1.8 : 1; ctx.beginPath(); ctx.moveTo(from.point.x, from.point.y); ctx.lineTo(to.point.x, to.point.y); ctx.stroke(); ctx.restore(); }); nodes.forEach(node => { const color = space.colors[node.domain] || '#94A3B8', radius = 4 + Number(node.strength || .4) * 8; ctx.save(); ctx.translate(node.point.x, node.point.y); ctx.globalAlpha = node.temporal_bucket === 'current' ? .96 : .38; ctx.fillStyle = ctx.strokeStyle = color; ctx.shadowColor = color; ctx.shadowBlur = node.id === selectedId ? 20 : 5; ctx.beginPath(); drawShape(ctx, node, radius); node.kind === 'recommendation' ? ctx.stroke() : ctx.fill(); ctx.restore(); if (width > 620 && !node.masked) { ctx.globalAlpha = node.temporal_bucket === 'current' ? .88 : .45; ctx.fillStyle = '#e8efeb'; ctx.font = '11px system-ui'; ctx.fillText(String(node.label).slice(0, 20), node.point.x + radius + 4, node.point.y + 3); } }); canvas._nodes = nodes; const fallback = $('personal-space-fallback'); if (fallback) { fallback.innerHTML = nodes.length ? nodes.slice(0, 30).map(node => `<button type="button" data-space-node="${esc(node.id)}">${esc(node.label)} · ${esc(domainLabel(node.domain))} · ${esc(nodeKindLabel(node.kind))} · ${esc(temporalLabel(node.temporal_bucket))}</button>`).join('') : '<div class="empty-state"><p>表示できる記憶や判断はまだありません。</p><button type="button" class="secondary" data-space-record>記録する</button></div>'; fallback.querySelectorAll('[data-space-node]').forEach(button => button.addEventListener('click', () => selectNode(nodes.find(node => node.id === button.dataset.spaceNode)))); fallback.querySelector('[data-space-record]')?.addEventListener('click', () => window.personalOsNavigate?.('home')); } }
  async function selectNode(node) { if (!node) return; selectedId = node.id; const detail = $('personal-space-detail'); if (detail) detail.innerHTML = `<b>${esc(node.label)}</b><div class="meta">${esc(domainLabel(node.domain))} · ${esc(nodeKindLabel(node.kind))} · ${esc(temporalLabel(node.temporal_bucket))}</div><p>詳細を読み込み中です。</p>`; renderSpace(); const parts = String(node.id).split('-'); const kind = node.id.startsWith('result-plan-') ? 'plan' : node.id.startsWith('result-event-') ? 'result' : parts[0], id = parts.at(-1); try { const response = await fetch(`/api/personal-space/nodes/${encodeURIComponent(kind)}/${encodeURIComponent(id)}?include_sensitive=${$('space-sensitive')?.checked ? 'true' : 'false'}`), data = await response.json(); if (!response.ok) throw new Error(data.error || '詳細を取得できません'); const text = data.masked ? '機微情報はマスク中です。' : [data.summary, data.rationale, data.result, data.source_document].filter(Boolean).map(esc).join('<br>') || '追加の説明はありません。'; if (detail) detail.innerHTML = `<b>${esc(node.label)}</b><div class="meta">${esc(domainLabel(node.domain))} · ${esc(nodeKindLabel(node.kind))} · ${esc(temporalLabel(node.temporal_bucket))}</div><p>${text}</p>`; } catch (error) { if (detail) detail.textContent = error.message; } }
  async function refreshSpace() { const detail = $('personal-space-detail'); try { const showSensitive = $('space-sensitive')?.checked ? 'true' : 'false', response = await fetch(`/api/personal-space?include_sensitive=${showSensitive}&limit=220`); if (!response.ok) throw new Error('Personal Spaceを取得できません'); space = await response.json(); const select = $('space-domain'); if (select && !select.dataset.ready) { Object.keys(space.colors).filter(key => key !== 'lifestyle').forEach(key => { const option = document.createElement('option'); option.value = key; option.textContent = domains[key] || key; select.append(option); }); select.dataset.ready = 'true'; } const legend = $('personal-space-legend'); if (legend) legend.innerHTML = Object.entries(space.colors).filter(([key]) => key !== 'lifestyle').map(([key, color]) => `<span><i class="space-dot" style="background:${color}"></i>${esc(domains[key] || key)}</span>`).join(''); renderSpace(); } catch (error) { if (detail) detail.textContent = error.message; } }
  const number = value => value == null ? '—' : Number(value).toLocaleString('ja-JP', { maximumFractionDigits: 2 });
  const compatibilityLabels = { exact: '同条件で比較可能', comparable: '単位変換して比較可能', reference_only: '参考値', incompatible: '比較できません' };
  const statisticLabels = { mean: '平均', median: '中央値', percentile: 'パーセンタイル', distribution: '分布', proportion: '割合', count: '件数', index: '指数' };
  const sourceTypeLabels = { official: '公的統計', quasi_official: '公的機関系', research: '調査・研究', other: 'その他', sample: 'デモ' };
  const benchmarkReasonLabels = { no_confirmed_current_fact: '比較できる現在情報がありません', personal_contract_missing: '本人情報の比較定義が未登録です', metric_key_mismatch: '指標の定義が異なります', metric_key_unverified: '指標の対応を確認できません', statistical_unit_mismatch: '個人値・世帯値など、比較対象が異なります', statistical_unit_unverified: '比較対象の単位を確認できません', measurement_kind_mismatch: '残高・年間フローなど、測定方法が異なります', measurement_kind_unverified: '測定方法を確認できません', time_basis_mismatch: '月次・年次・時点など、期間が異なります', time_basis_unverified: '期間条件を確認できません', personal_unit_unverified: '本人情報の単位を確認できません', reference_unit_unverified: '比較データの単位を確認できません', embedded_contract_conflicts_with_registry: '保存済みの古い比較条件と、現在の正式な比較定義が一致しません' };
  function reasons(reasonsList) { return (reasonsList || []).map(reason => benchmarkReasonLabels[reason.code] || '比較条件を確認できません').filter(Boolean).join(' / '); }
  function compatibility(comparison, statisticType) { const label = compatibilityLabels[comparison?.compatibility] || '比較条件を確認できません'; if (!comparison) return `<span class="pill">${label}</span>`; if (['exact', 'comparable'].includes(comparison.compatibility)) { const statistic = statisticLabels[statisticType] || '統計値'; return `<span class="pill">${label}</span><br>あなた: <b>${number(comparison.personal_value)} ${esc(comparison.unit || '')}</b><br>${statistic}との差: ${number(comparison.absolute_difference)}`; } return `<span class="pill">${label}</span><br>${esc(reasons(comparison.reasons))}`; }
  function percentileBandLabel(band) { return ({ below_p10: '下位10%未満', p10_p25: '10〜25%の範囲', p25_p50: '25〜50%の範囲', p50_p75: '50〜75%の範囲', p75_p90: '75〜90%の範囲', above_p90: '上位10%超' })[band] || '分布上の位置は未判定です'; }
  function distribution(observation, comparison) { const d = observation?.distribution; if (!d || typeof d !== 'object') return ''; const keys = ['p10', 'p25', 'p50', 'p75', 'p90']; if (!keys.every(key => d[key] != null)) return '<p class="help">分布上の位置は未判定です。</p>'; return `<div class="benchmark-band">${keys.map(key => `<span>${key.toUpperCase()}<b>${number(d[key])}</b></span>`).join('')}</div><p class="help">あなたの位置: ${esc(percentileBandLabel(comparison?.percentile_band))}</p>`; }
  function groupBenchmarks(series) { return Object.values(series.reduce((groups, row) => { const key = row.comparison_group_key || `series:${row.id}`; (groups[key] ||= []).push(row); return groups; }, {})); }
  async function refreshBenchmarks() { const target = $('benchmark-series'); if (!target) return; try { const response = await fetch('/api/benchmarks'), payload = await response.json(); $('benchmark-privacy').textContent = payload.privacy || ''; target.innerHTML = groupBenchmarks(payload.series || []).map(group => { const primary = group[0], cards = group.map(series => { const observation = series.observations?.[0], value = observation?.value == null ? '分布データ' : `${number(observation.value)} ${series.unit}`; return `<div class="benchmark-stat"><b>${esc(statisticLabels[series.statistic_type] || '統計値')}</b><div>${esc(value)}</div>${compatibility(series.comparison, series.statistic_type)}</div>${distribution(observation, series.comparison)}`; }).join(''); return `<article class="benchmark-card"><div class="benchmark-card-head"><h3>${esc(primary.metric_name)}</h3>${primary.is_demo ? '<span class="pill">デモ</span>' : ''}</div>${cards}<div class="benchmark-meta">出典種別: ${esc(sourceTypeLabels[primary.source_type] || 'その他')}<br>出典: <a href="${esc(primary.source_url)}" target="_blank" rel="noopener">${esc(primary.publisher)}</a><br>比較対象: ${esc(primary.population_scope)} · 対象時期: ${esc(primary.observations?.[0]?.reference_period || '')}<br>定義: ${esc(primary.definition)}<details><summary>詳細な比較条件</summary><div>地域: ${esc(primary.geography || '未記録')} / 版: ${esc(primary.version || '未記録')}</div></details></div></article>`; }).join('') || '<p class="help">参照データはまだありません。</p>'; } catch (error) { target.textContent = error.message; } }
  function rawBundle() { return { raw_json: $('benchmark-import-json')?.value || '' }; }
  async function previewBundle() { const output = $('benchmark-preview-result'); try { const response = await fetch('/api/benchmarks/preview', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(rawBundle()) }), data = await response.json(); if (!response.ok) throw new Error(data.error || '検証できませんでした'); output.textContent = `${data.datasets} データセット / ${data.observations} 観測値: ${(data.metrics || []).join(' · ')}`; } catch (error) { output.textContent = error.message; } }
  async function importBundle(event) { event.preventDefault(); const notice = $('benchmark-import-notice'); try { const response = await fetch('/api/benchmarks/import', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(rawBundle()) }), data = await response.json(); if (!response.ok) throw new Error(data.error || '保存できませんでした'); notice.textContent = `保存しました（${data.datasets}件）`; refreshBenchmarks(); } catch (error) { notice.textContent = error.message; } }
  async function loadDemo() { const response = await fetch('/api/benchmarks/demo', { method: 'POST' }), data = await response.json(); $('benchmark-import-notice').textContent = response.ok ? 'DEMOデータを追加しました。相談には使いません。' : (data.error || '追加できませんでした'); if (response.ok) refreshBenchmarks(); }
  async function copyPrompt() { const prompt = '公開統計からPopulation Benchmark BundleのJSONだけを作成してください。個人情報は含めないでください。bundle_version,datasets,source_name,publisher,source_url,source_type,methodology,metric_key,metric_name,domain,unit,statistic_type,definition,population_scope,segment_definition,metric_contract(metric_key/statistical_unit/measurement_kind/time_basis/canonical_unit),observations(reference_period,published_at,value,sample_size,distribution,revision)を含めてください。total_assetsとfinancial_assets、individualとhousehold、meanとmedianを混同しないでください。'; try { await navigator.clipboard.writeText(prompt); $('benchmark-import-notice').textContent = 'ChatGPT用プロンプトをコピーしました。'; } catch { $('benchmark-import-notice').textContent = 'コピーできませんでした。'; } }
  const timelineKindLabels = { fact_started: '新しく記録', fact_changed: '情報を更新', preference_changed: '好みが変化', plan_created: '計画を作成', plan_changed: '計画を更新', decision_created: '判断候補を追加', decision_made: '判断', executed: '実行', result_recorded: '結果を記録', evaluation_recorded: '後日評価', travel_event: '旅行', housing_event: '住居', finance_snapshot: '資産', relationship_event: '人間関係', inference_confirmed: '確認済みの傾向' };
  const timelineKindLabel = kind => timelineKindLabels[String(kind || '').toLowerCase()] || '記録';
  let timelineCursor = null, timelineEventMap = new Map();
  const timelineDate = value => {
    if (!value) return '日時不明';
    const date = new Date(value.length === 10 ? `${value}T00:00:00` : value);
    return Number.isNaN(date.getTime()) ? String(value).slice(0, 10) : new Intl.DateTimeFormat('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' }).format(date);
  };
  function timelineQuery(reset = false) {
    const params = new URLSearchParams({ limit: '30' });
    const period = $('timeline-period')?.value || 'all';
    if (period !== 'all') {
      const days = { month: 31, quarter: 93, year: 366 }[period] || 0;
      const since = new Date(); since.setDate(since.getDate() - days);
      params.set('from', since.toISOString().slice(0, 10));
    }
    const domain = $('timeline-domain')?.value || '', kind = $('timeline-kind')?.value || '';
    if (domain) params.set('domain', domain);
    if (kind) params.set('kind', kind);
    if (!reset && timelineCursor) params.set('cursor', timelineCursor);
    return params;
  }
  function timelineEmpty() { return '<div class="empty-state"><p>まだ表示できる変化がありません。</p><span class="help">記録や判断を続けると、ここに「以前と今の違い」が表示されます。</span><div class="actions"><button type="button" class="secondary" data-timeline-record>記録する</button><button type="button" class="secondary" data-timeline-today>今日を見る</button></div></div>'; }
  function timelineCard(event) {
    const source = event.temporal_source === 'created_at' ? '記録日時' : '';
    return `<article class="timeline-event" data-timeline-event="${esc(event.id)}" tabindex="0" role="button" aria-label="${esc(event.title)}の詳細を見る"><div class="timeline-marker" aria-hidden="true"></div><div class="timeline-event-date">${esc(source ? `${source} ${timelineDate(event.occurred_at)}` : timelineDate(event.occurred_at))}</div><div class="timeline-event-main"><div class="timeline-event-meta"><span class="pill">${esc(timelineKindLabel(event.event_kind))}</span><span>${esc(domainLabel(event.domain))}</span></div><h3>${esc(event.title)}</h3><p>${esc(event.summary || '内容を確認できます')}</p>${event.action?.type === 'open_decision' ? '<span class="timeline-action">判断を見る</span>' : ''}</div></article>`;
  }
  function bindTimelineActions(target) {
    target.querySelectorAll('[data-timeline-record]').forEach(button => button.addEventListener('click', () => window.personalOsNavigate?.('home')));
    target.querySelectorAll('[data-timeline-today]').forEach(button => button.addEventListener('click', () => window.personalOsNavigate?.('today')));
    target.querySelectorAll('[data-timeline-event]').forEach(card => {
      const open = () => openTimelineDetail(card.dataset.timelineEvent, card);
      card.addEventListener('click', open);
      card.addEventListener('keydown', event => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); open(); } });
    });
  }
  async function refreshTimeline({ append = false } = {}) {
    const target = $('timeline-list'), more = $('timeline-more'); if (!target) return;
    if (!append) { timelineCursor = null; target.setAttribute('aria-busy', 'true'); target.innerHTML = '<p class="help">変化を読み込んでいます…</p>'; }
    try {
      const response = await fetch(`/api/timeline?${timelineQuery(!append)}`), payload = await response.json();
      if (!response.ok) throw new Error(payload.error || '変化を読み込めませんでした');
      const events = Array.isArray(payload.events) ? payload.events : [];
      if (!append) timelineEventMap = new Map();
      events.forEach(event => timelineEventMap.set(event.id, event));
      if (!append) target.innerHTML = events.length ? events.map(timelineCard).join('') : timelineEmpty();
      else target.insertAdjacentHTML('beforeend', events.map(timelineCard).join(''));
      bindTimelineActions(target);
      timelineCursor = payload.next_cursor || null;
      if (more) more.hidden = !timelineCursor;
    } catch {
      target.innerHTML = '<div class="empty-state"><p>変化を読み込めませんでした。</p><span class="help">入力内容や記録は失われていません。</span><div class="actions"><button type="button" class="secondary" data-timeline-reload>再読み込み</button></div></div>';
      target.querySelector('[data-timeline-reload]')?.addEventListener('click', () => refreshTimeline());
    } finally { target.removeAttribute('aria-busy'); }
  }
  async function openTimelineDetail(eventId, opener) {
    const content = $('timeline-detail-content'); if (!content) return;
    content.innerHTML = '<p class="help">詳細を読み込んでいます…</p>';
    window.personalOsSheets?.open('timeline-detail-sheet', opener);
    try {
      let event = timelineEventMap.get(eventId);
      if (!event) {
        const response = await fetch(`/api/timeline/${encodeURIComponent(eventId)}`);
        event = await response.json();
        if (!response.ok) throw new Error(event.error || '詳細を読み込めませんでした');
      }
      const detail = event.detail || {};
      const comparison = detail.previous_value && detail.new_value ? `<section><h3>変更</h3><p>${esc(detail.previous_value)} → ${esc(detail.new_value)}</p></section>` : '';
      const basis = (event.basis || []).length ? '<section><h3>根拠</h3><p class="help">確認済みの記録・判断をもとに表示しています。</p></section>' : '<section><h3>根拠</h3><p class="help">確認できる根拠はまだありません。</p></section>';
      const compare = event.event_kind === 'fact_changed' ? `<button type="button" class="secondary" data-timeline-compare="${esc(event.id)}">今と比べる</button>` : '';
      const decision = event.action?.type === 'open_decision' ? '<button type="button" class="secondary" data-timeline-decision>同じ判断を見る</button>' : '';
      content.innerHTML = `<p class="meta">${esc(timelineDate(event.occurred_at))} · ${esc(timelineKindLabel(event.event_kind))} · ${esc(domainLabel(event.domain))}</p><h3>${esc(event.title)}</h3><p>${esc(event.summary || '内容を確認できます')}</p>${comparison}${basis}<p class="help">${detail.currentness === 'current' ? '現在の情報です。' : detail.currentness ? '過去の情報です。' : ''}</p><div class="actions">${compare}${decision}</div>`;
      content.querySelector('[data-timeline-decision]')?.addEventListener('click', () => {
        const decisionId = Number(event.action?.decision_id || event.action?.id || event.detail?.decision_id || 0);
        if (decisionId && typeof window.personalOsOpenDecisionReplay === 'function') {
          window.personalOsSheets?.close($('timeline-detail-sheet'));
          window.personalOsOpenDecisionReplay(decisionId);
        } else window.personalOsNavigate?.('decisions');
      });
      content.querySelector('[data-timeline-compare]')?.addEventListener('click', () => {
        const prompt = `${timelineDate(event.occurred_at)}の${domainLabel(event.domain)}に関する記録と、現在の${domainLabel(event.domain)}の情報を比較してください`;
        window.personalOsSheets?.close($('timeline-detail-sheet'));
        window.personalOsNavigate?.('chat');
        window.setTimeout(() => { const input = $('chat-message'); if (input) { input.value = prompt; input.focus(); input.dispatchEvent(new Event('input', { bubbles: true })); } }, 0);
      });
    } catch { content.innerHTML = '<p>詳細を読み込めませんでした。</p>'; }
  }
  function wireGestures() { const canvas = $('personal-space-canvas'); if (!canvas) return; let drag = null, pinch = null; const reset = $('space-reset') || (() => { const button = document.createElement('button'); button.id = 'space-reset'; button.type = 'button'; button.className = 'secondary'; button.textContent = '表示をリセット'; canvas.parentElement.before(button); return button; })(); reset.addEventListener('click', () => { camera = { yaw: .16, pitch: -.18, zoom: 1, panX: 0, panY: 0 }; renderSpace(); }); canvas.addEventListener('wheel', event => { event.preventDefault(); camera.zoom = Math.max(.5, Math.min(2.4, camera.zoom * (event.deltaY > 0 ? .9 : 1.1))); renderSpace(); }, { passive: false }); canvas.addEventListener('pointerdown', event => { canvas.setPointerCapture?.(event.pointerId); drag = { x: event.clientX, y: event.clientY, pan: event.shiftKey, distance: 0 }; }); canvas.addEventListener('pointermove', event => { if (!drag) return; const dx = event.clientX - drag.x, dy = event.clientY - drag.y; drag.x = event.clientX; drag.y = event.clientY; drag.distance += Math.hypot(dx, dy); if (drag.pan) { camera.panX += dx; camera.panY += dy; } else { camera.yaw += dx * .012; camera.pitch = Math.max(-1.1, Math.min(1.1, camera.pitch + dy * .012)); } renderSpace(); }); const finish = event => { if (!drag) return; const moved = drag.distance > 5; drag = null; if (moved) return; const rect = canvas.getBoundingClientRect(), x = event.clientX - rect.left, y = event.clientY - rect.top, nearest = (canvas._nodes || []).map(node => ({ node, distance: Math.hypot(node.point.x - x, node.point.y - y) })).sort((a, b) => a.distance - b.distance)[0]; if (nearest?.distance <= 30) selectNode(nearest.node); }; canvas.addEventListener('pointerup', finish); ['pointercancel', 'lostpointercapture'].forEach(type => canvas.addEventListener(type, () => { drag = null; })); canvas.addEventListener('touchstart', event => { if (event.touches.length === 2) pinch = Math.hypot(event.touches[0].clientX - event.touches[1].clientX, event.touches[0].clientY - event.touches[1].clientY); }, { passive: true }); canvas.addEventListener('touchmove', event => { if (event.touches.length !== 2 || !pinch) return; const size = Math.hypot(event.touches[0].clientX - event.touches[1].clientX, event.touches[0].clientY - event.touches[1].clientY); camera.zoom = Math.max(.5, Math.min(2.4, camera.zoom * size / pinch)); pinch = size; renderSpace(); }, { passive: true }); canvas.addEventListener('touchcancel', () => { pinch = null; }); }
  function refreshExploreMode(mode) { if (mode === 'timeline') return refreshTimeline(); if (mode === 'benchmark') return refreshBenchmarks(); return refreshSpace(); }
  function wire() { document.querySelectorAll('[data-explore-mode]').forEach(button => button.addEventListener('click', () => { const mode = button.dataset.exploreMode; ['space', 'benchmark', 'timeline'].forEach(name => { $(`explore-${name}`)?.classList.toggle('hidden', name !== mode); document.querySelector(`[data-explore-mode="${name}"]`)?.classList.toggle('active', name === mode); }); refreshExploreMode(mode); })); ['space-search', 'space-domain', 'space-kind', 'space-current', 'space-history', 'space-edges'].forEach(id => $(id)?.addEventListener(id === 'space-search' ? 'input' : 'change', renderSpace)); $('space-sensitive')?.addEventListener('change', refreshSpace); ['timeline-period', 'timeline-domain', 'timeline-kind'].forEach(id => $(id)?.addEventListener('change', () => refreshTimeline())); $('timeline-more')?.addEventListener('click', () => refreshTimeline({ append: true })); $('benchmark-preview')?.addEventListener('click', previewBundle); $('benchmark-import-form')?.addEventListener('submit', importBundle); $('benchmark-load-demo')?.addEventListener('click', loadDemo); $('benchmark-copy-prompt')?.addEventListener('click', copyPrompt); addEventListener('resize', renderSpace); wireGestures(); }
  window.refreshPersonalSpace = refreshSpace; window.refreshBenchmarks = refreshBenchmarks; window.refreshTimeline = refreshTimeline; window.refreshExplore = () => refreshExploreMode(document.querySelector('[data-explore-mode].active')?.dataset.exploreMode || 'space');
  document.addEventListener('DOMContentLoaded', wire);
}());
